@model PageViewModel

<div class="modal" id="pagealert" role="dialog" aria-describedby="Contains meta information about the current page">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="default-header">@Model.Title</h4>
            </div>

            @using (Html.BeginForm("Index", "Home", FormMethod.Post))
            {
                <div class="modal-body">
                    <label for="HeadContent">@SiteStrings.Page_Add_Your_Comment</label>
                    <textarea id="commentarea" name="commentarea" tabindex="0" class="form-control" rows="6" cols="20" rel="popover" data-content="Any additional HTML or scripts you enter here will be added to the &amp;lt;head&amp;gt; tag of the page." placeholder="Enter a comment - optional"></textarea> <!--localization-->
                </div>

                <div class="modal-footer">
                    <button id="buttonrating" type="button" data-dismiss="modal" class="btn btn-primary">@SiteStrings.Button_Confirm</button>
                </div>
            }
        </div>
    </div>
</div>